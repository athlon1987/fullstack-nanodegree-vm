/*
 * SwimPath — Firebase configuration.
 *
 * Paste your Firebase web-app config below (Firebase console →
 * Project settings → "Your apps" → SDK setup and configuration → Config).
 *
 * NOTE: these values are NOT secrets — they identify your project, not
 * authorise access. Privacy is enforced by Firestore security rules plus the
 * unguessable team code. It is safe to commit this file publicly.
 *
 * While the fields are blank, the app runs fully offline (localStorage only)
 * and the sharing features stay hidden.
 */
export const firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
